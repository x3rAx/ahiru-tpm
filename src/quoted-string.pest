quoted_string = { single_quoted | double_quoted }

single_quoted = { "'" ~ (!"'" ~ ANY)* ~ "'" }
double_quoted = { "\"" ~ (escaped_char | normal_char)* ~ "\"" }

escaped_char = { "\\" ~ "\"" }
normal_char  = { !("\"") ~ ANY }
